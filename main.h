#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>
#include <stdio.h>

int print_char(char c);
int _putchar(char c);
int _printf(const char *format, ...);
int int_printf(const char *format, ...);

#endif /*MAIN_H */
